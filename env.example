# Configuration de l'environnement pour XC Afrique Backend
# Copiez ce fichier vers .env et modifiez les valeurs selon vos besoins

# Port du serveur
PORT=5000

# Environnement (development, production)
NODE_ENV=development

# URL de la base de données MongoDB
# Format: mongodb://localhost:27017/xcafrique
# Ou pour MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/xcafrique
MONGODB_URI=mongodb://localhost:27017/xcafrique

# Secret pour JWT (générer une chaîne aléatoire sécurisée)
# Vous pouvez générer un secret avec: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=votre_secret_jwt_tres_securise_changez_moi
JWT_EXPIRE=7d

# URL du frontend React (pour CORS)
# Pour plusieurs origines, séparer par des virgules: http://localhost:3000,http://localhost:5173
# Par défaut, autorise: localhost:3000, localhost:5173, localhost:5174, localhost:5175
FRONTEND_URL=http://localhost:3000

# Configuration pour ContentManager (génération automatique de contenu)
# URL de base de l'API (par défaut: http://localhost:5000/api)
API_BASE_URL=http://localhost:5000/api

# Token JWT pour l'authentification automatique (optionnel)
# Si non défini, le ContentManager s'authentifiera avec ADMIN_EMAIL et ADMIN_PASSWORD
CURSOR_TOKEN=

# Identifiants admin pour l'authentification automatique
ADMIN_EMAIL=admin@xcafrique.com
ADMIN_PASSWORD=admin123

# Configuration des emails
CONTACT_EMAIL=contact@xcafrique.org
NEWSLETTER_EMAIL=news@xcafrique.org

# Configuration SMTP pour l'envoi d'emails
# Exemple pour Hostinger (recommandé):
# SMTP_HOST=smtp.hostinger.com
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=contact@xcafrique.org
# SMTP_PASSWORD=votre_mot_de_passe_email
#
# Exemple pour Gmail: smtp.gmail.com (port 587, SMTP_SECURE=false)
# Exemple pour SendGrid: smtp.sendgrid.net
# Exemple pour Mailgun: smtp.mailgun.org
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=contact@xcafrique.org
SMTP_PASSWORD=votre_mot_de_passe_email

